<div class="main">
  <div class="container">
    <!-- まずはもらった側の感想投稿できるようにする。できたら感想を贈った人に送信できるように -->


https://github.com/thoughtbot/paperclip のEdit and New Viewのところから

    <%= form_tag("/posts/create",{ multipart: true} ) do %>
    <div class="form-group">
      <label>シチュエーション</label>
      <input name="situation" type="text" class="form-control" placeholder="何のプレゼント？ 例：卒業のお祝い">
    </div>
    <div class="form-group">
      <label>もらったもの</label>
      <input name="goods" type="text" class="form-control" placeholder="どんなアイテム？ 例：Beamsのクリスマス柄の靴下">
    </div>
    <div class="form-group">
      <label>コメント</label>
      <textarea name="comment" class="form-control" placeholder="何が特に嬉しかった？ 例：日常の中で欲しいと言っていたものをさりげなく覚えておいてくれたこと"></textarea>
    </div>
    <div class="form-group input-group">
      <label class="input-group-btn">
        <span class="btn btn-primary">
          画像を選択する<input name="post_image" type="file" style="display:none">
        </span>
      </label>
      <input id="show_uploaded_imagename" type="text" class="form-control" readonly="">
      <!--選択した画像名を表示するためのjavascript-->
      <script type="text/javascript">
        $(document).on('change', 'input[type=file]', function () {
          var input = $(this);
          label = input.val().replace(/\\/g, '/').replace(/.*\//, '');
          $("#show_uploaded_imagename").val(label);
        });
      </script>

    </div>
    <div class="form-group">
      <button type="submit" class="btn btn-success btn-sm">投稿する</button>
      <button type="submit" class="btn btn-warning btn-sm">
        プレゼント贈り主に気持ちを送信する
        <span class="small">(投稿されます)</span>
      </button>
      <!-- TODO twitterかLINEに送信できるようにする。 -->
      <!-- TODO DBに保存されるように修正する -->
    </div>
  <% end %>

</div>
</div>
